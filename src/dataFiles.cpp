#include <string>

#include "dataFiles.h"

namespace GPS
{
  namespace DataFiles
  {
      const std::string dataDir = "../data/";
      const std::string NMEADir  = dataDir + "NMEA/";
      const std::string GPXRoutesDir = dataDir + "GPX/routes/";
      const std::string GPXTracksDir = dataDir + "GPX/tracks/";
  }
}
